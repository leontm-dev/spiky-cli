import { SpikyConfig } from '../../types/SpikyConfig.js';
import { FunctionIndexes } from '../../types/functionIndex.js';
export default function checkForOutput(outputConfig: SpikyConfig['export'], functionIndexes: FunctionIndexes): boolean;
